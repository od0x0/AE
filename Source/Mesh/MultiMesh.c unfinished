#include "../MultiMesh.h"

AEMM* AEMMNew(void){
	return calloc(1,sizeof(AEMM));
}

void AEMMAdd(AEMM* mm,unsigned int key,AEVBO* vbo){
	
}

AEMM* AEMMCompile(AEMM* mm){
/*	AEMM* result=AEMMNew();
	for(unsigned int i=0;i<mm->setCount;i++){
		
	}*/
	return mm;
}

void AEMMDraw(AEMM* mm,unsigned int set){
	
}

void AEMMDelete(AEMM* mm){
	if(mm==NULL) return;
	AEVBODelete(mm->vbo);
	free(mm->keys);
	free(mm->lastIndexOfSets);
	free(mm);
}